type Student @table(key: ["id"]) {
  id: UUID! @default(expr: "uuidV4()")
  name: String!
  type: String! # "online" or "onsite"
  gender: String! # "male" or "female"
  dob: Date!
  school: String!
  email: String!
  phoneNo: String!
  address: String!
  status: String!
  tuitionLoc: String!
  tutor: Tutor!
}

type Tutor @table(key: "id") {
  id: String! @default(expr: "auth.uid")
  name: String!
  type: String # "online" or "onsite"
  gender: String # "male" or "female"
  email: String
  phoneNo: String
  address: String
}

type StudentFormURL @table(key: ["id"]) {
  id: UUID! @default(expr: "uuidV4()")
  tutor: Tutor!
  createdAt: Timestamp! @default(expr:"request.time")
  submitted: Boolean! @default(value: false)
}

# add logic
type StudentSubject @table(key:["student","subject"]) {
  student: Student!
  subject: String!
}


